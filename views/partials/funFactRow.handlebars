  <tr>
    <td><a href="#" onClick="updateSale('this.funFactID')">Edit</a></td>
    <td><a href="#" onclick="deleteSale('this.funFact')">Delete</a></td>  <!-- doesn't work because in a seperate file? -->
    <td align="right" >{{funFactID}}</td>
    <td >{{funFact}}</td>
  </tr>